package test {
	import de.polygonal.ds.BitVector;

	import flash.display.Sprite;

	/**
	 * @author ssen(i@ssen.name)
	 */
	public class DSBitVectorTest extends Sprite {
		public function DSBitVectorTest() {
			// 뭐에 쓰는건지 잘 모르겠네...;;;
			var bv : BitVector = new BitVector();
		}
	}
}
