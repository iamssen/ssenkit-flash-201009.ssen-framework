package ssen.debug.autocase {
import ssen.core.MathX;

/**
 * @author ssen (i@ssen.name)
 */
public class RandomBoolean implements IRandomValue {
	public function getRandom() : * {
		return MathX.rand(0, 1) > 0;
	}
}
}
