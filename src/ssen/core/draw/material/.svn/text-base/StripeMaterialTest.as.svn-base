package ssen.core.draw.material {
import flash.display.Sprite;

/**
 * @private
 * @author ssen (i@ssen.name)
 */
public class StripeMaterialTest extends Sprite {
	public function StripeMaterialTest() {
		new StripeMaterial(Vector.<StripeLine>([new StripeLine(4, 0xaaaaaa), new StripeLine(4, 0x000000, 0)])).draw(graphics, 10, 10, 400, 1);
		new StripeMaterial(Vector.<StripeLine>([new StripeLine(5, 0xaaaaaa), new StripeLine(3, 0x000000, 0.1)]), true).draw(graphics, 10, 14, 1, 300);
	}
}
}
